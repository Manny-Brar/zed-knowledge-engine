---
description: ZED verification reminder — run tests after code changes
globs: ["**/*test*", "**/*spec*", "**/*.test.*"]
---

When modifying test files or code with existing tests:
1. Run the relevant test suite to verify changes
2. If tests fail, fix them before moving on
3. Do NOT mark work as complete with failing tests
