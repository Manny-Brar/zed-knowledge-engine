---
description: Search the knowledge graph with graph-boosted ranking
---

Use the `zed_search` MCP tool to search the knowledge graph for "$ARGUMENTS".

Present results clearly with titles, scores, and backlink counts. If the user's query is broad, also use `zed_related` on the top result to show connected knowledge.

If no results are found, suggest alternative search terms or recommend creating a new note with `zed_write_note`.
