---
description: Open or create today's daily session note
---

Run `zed daily` via the Bash tool to get today's session note.

If the note already exists, display its contents. If it's new, confirm it was created.

If the user provided additional context with "$ARGUMENTS", append it to today's note using the `append` parameter.
