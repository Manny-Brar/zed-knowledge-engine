---
description: Browse knowledge by tags — list all tags or filter by a specific tag
---

Use the `ke_tags` MCP tool to browse knowledge by tags.

If "$ARGUMENTS" is provided, use it as the tag to filter by. Otherwise, list all tags with note counts.

This helps users navigate their knowledge graph by topic rather than text search.
